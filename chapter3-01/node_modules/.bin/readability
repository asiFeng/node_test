#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_node-readability@3.0.0@node-readability/src/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../_node-readability@3.0.0@node-readability/src/cli.js" "$@"
  ret=$?
fi
exit $ret
